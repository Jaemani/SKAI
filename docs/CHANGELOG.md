# 아키텍처·온톨로지 변경로그

루트 기획문서(특히 `ontology.md`·`architecture.md`)에 대한 모든 변경을 기록.
형식: `날짜 · [태그] 변경 요약 — 이유 (관련 DR)` · 태그: `[ontology]` `[architecture]` `[pipeline]` `[docs]`

---

## 2026-07-04

- [docs] 문서 체계 분리 신설: `docs/decisions/`(의사결정) · `docs/CHANGELOG.md`(이 파일) · `docs/worklog/`(실행 로그) — 용도별 분리 사용자 지시 (DR-0001)
- [ontology] **v0.1 기준선 확정** = `ontology.md` 초판: 객체 11종(Aircraft·Observation·Track·Satellite·OrbitPass·Region·WeatherState·NewsEvent·Operator·Anomaly·SituationAssessment) · 링크 11종 · 액션 4종(CreateAnomaly·Confirm/DismissAnomaly·GenerateSituationAssessment·SetRegionAlertLevel). 이후 온톨로지 변경은 전부 이 로그에 기록하고 스멜테스트(ontology.md §0) 재통과 확인.
- [architecture] v0.1 기준선 = `architecture.md` 초판 (AIP-spine, 커넥터 4종, 이상탐지 7유형, 폴백=SQLite 동일 스키마)
- [docs] `aip-integration.md` P0-B 검증 반영: §0-보강 신설(스키마 생성 UI 전용·OSDK private index 설치·Python <3.13 함정·토큰 발급 경로) + §3 설치/인증 스니펫 정정 — opus 에이전트 웹조사 검증분, 출처는 `docs/worklog/P0B-foundry.md` (DR-0002)
- [pipeline] Foundry 왕복 BLOCKED 판정에 따른 경로 결정: 사용자 UI 액션 4개 대기 + Python 3.12 venv 선제 준비 + 저수준 `foundry_sdk`(3.14 지원)를 대체 경로 후보로 채택. **폴백(SQLite) 전환 아님** — AIP-spine 유지 (DR-0002)
- [docs] `data-sources.md` 4소스 실응답 확인 소절 추가(P0-A 검증: OpenSky/Celestrak/METAR OK, GDELT 5초 간격 조건부) — sonnet 에이전트 실측분
- [architecture] P1 구현 형태 확정: 저장 어댑터 분리(`OntologyStore` 인터페이스 + local SQLite 미러 + foundry 스텁), provenance 강제를 store 레벨에 선행 구현, P1 프론트는 FastAPI+vanilla Leaflet(React/Vite는 P4 재판단) (DR-0003)
- [pipeline] **P1 수직관통 완료·검증**: `connectors/` `ontology/` `server/` `web/` `tests/` 신설. 온톨로지 v0.1 중 Aircraft·Observation·Track·Region + 링크 2종(observed_as·composed_of) 코드 구현. 테스트 14/14(provenance 거부·gap 판정·매핑), 라이브 3사이클 = Aircraft 32·Observation 71(dedup)·Track 32, 지도 렌더 검증(`docs/worklog/p1_map.png`). 프론트 read가 OSDK 대신 FastAPI→store_local인 것은 DR-0003 승인 편차 — Foundry 개통 시 원복
- [ontology] Anomaly 객체 + evidenced_by/involves 링크 + CreateAnomaly/Confirm/Dismiss 액션의 코드 구현 착수(P2) — 스키마는 v0.1 그대로, 신규 정의 없음. 설명 생성은 ExplainerBackend 분리(template 기본/claude-cli 옵션/AIP Logic 스텁, DR-0004)
- [pipeline] **P2 완료·검증**: 비상 스쿽 끝단(룰→설명→CreateAnomaly[evidence 강제]→타임라인/지도→confirm/dismiss) 동작. 테스트 32/32, 합성 주입 3종(7500/7600/7700) end-to-end, 상태 전이 영속 확인(`docs/worklog/p2_anomaly.png`). evidence 없는 Anomaly는 store 레벨 EvidenceError로 전 경로 차단. 합성 주입도 source="synthetic" provenance 유지
- [architecture] P3 융합 확장 착수: 위성 카탈로그는 경량 그룹 한정 + TLE 12h 캐시, GDELT 5초 규율, 뉴스 confidence 저가중(≤0.4) + mentions 엔티티 링킹은 키워드 방식(해커톤 범위) (DR-0005)
- [pipeline] **P3 완료·검증**: 4종 소스가 한 온톨로지에 공존 (OpenSky 74obs·Celestrak 94위성/99통과창·METAR 1·GDELT 뉴스 14). 테스트 58/58, GMST 보정 테스트 증명, 통합 화면 확인(`docs/worklog/p3_fusion.png`). Satellite·OrbitPass·WeatherState·NewsEvent 객체 + of/over/mentions 링크 코드 구현 — 스키마 v0.1 그대로
- [ontology] provenance 강제 범위 확장: `validate_provenance` 덕타이핑화로 NewsEvent·WeatherState도 증거 객체로 강제. OrbitPass는 파생 계산 객체라 source_url 보존만(라이브 증거와 구분) — P3 실행 중 판단, 상세 `P3-fusion.md` §5
- [pipeline] 이월 이슈(P3→P4/P5): OrbitPass 재계산 stale 누적(future-pass 정리 필요·P4에서 수정), 뉴스↔항적 상관은 콜사인 링킹 아닌 시공간 버킷으로(P5), GDELT 라이브 취약 → P6 스냅샷 재생에 뉴스 포함
- [pipeline] **P4 완료·검증**: 자연어 질의→병렬 read→문장별 cites 강제 Assessment→채팅+서브그래프 SVG. 테스트 74/74, 질의 3개 end-to-end(전 문장 cites 유효 100%), 무근거 문장 혼입 시 전체 거부(SentenceEvidenceError)·무근거 질의는 no_evidence(환각 대신 침묵). OrbitPass stale 수정 포함 (`p4_copilot.png`·`p4_subgraph.png`, DR-0006)
- [ontology] **GenerateSituationAssessment 액션 구현**(v0.1 액션 4종 중 3종째 — SetRegionAlertLevel만 잔여) + **AssessmentSentence 물질화**: v0.1의 "cites=문장별 근거"(§2)를 문장 1급 레코드로 구체화. 스멜테스트 판단 — 문장→cites→객체의 provenance 그래프(기준4) 구현 수단이며 새 도메인 개념 아님(속성의 구조화). 스키마 의미 변경 없음
- [pipeline] **P5 완료·검증**: 이상탐지 5종(비상 스쿽+dropout·로이터링·군용기 접근·위성 근접 승격) + **correlated_with 링크 영속**(시공간 버킷 ±60분·공간 겹침) + "은닉 정황" 내러티브(dropout↔위성통과↔뉴스 — ontology.md §2 깊이 증명 그대로). 테스트 98/98, 평가 하네스: 합성 12시나리오 P/R 전 유형 1.00/1.00(결정적 세트 예상값·라이브 P/R은 별도), 맨몸 LLM 비교 = 파이프라인 문장 cites 100% vs 맨몸 검증가능 출처 0 (`p5_board.png`·`p5_narrative.png`, DR-0007)
- [ontology] correlated_with 링크 코드 구현으로 **v0.1 링크 11종 전부 사용 상태** 도달. dropout 신뢰도 이원화(교차 미확인 0.42/합성 미러 확인 0.72 — "단정 금지"의 코드화). OpArea 데모 소구역 Region 추가(classification 기존 값 사용 — 스키마 변경 아님)
- [pipeline] **P6 완료·검증 = PROMPTS P0~P6 전 단계 소화**: 데모 이중 모드(`demo.sh live|replay|stop`) + replay는 소켓 가드로 **네트워크 0 증명**(외부 egress 차단·luback만) + now 앵커링으로 연속 2회 바이트 동일 재현(SHA-256 일치). 테스트 106/106. demo.md 3분 대본(스텝별 화면·발화·폴백) + 심사 4항목 매핑 + 스토리보드 4장. Foundry는 "이관 준비 완료·크리덴셜 대기" 정직 문구 (DR-0008)
- [docs] 잔여: Foundry 사용자 UI 액션 4개(P0-B 크리티컬 패스) · git 커밋(사용자 결정 대기) · 발표 리허설. P6 잔여 리스크 3건은 `P6-demo.md` 참조
- [pipeline] **P0-B 완주(ROUNDTRIP-OK)**: 사용자 Foundry 개통(토큰·Aircraft Object Type·OSDK 발행) 후 write(create-aircraft 액션)→OSDK read-back 일치 검증. 하이브리드 경로 확정(read=생성 OSDK `skai_osdk_sdk` 0.1.0 / write=저수준 `foundry_sdk` Action.apply — 발행 OSDK에 Action 미포함·edits API는 AIP Function 전용이라). `aip-integration.md` §3 스니펫을 검증본으로 교체 (P0B-foundry.md §8)
- [ontology] Foundry 측 현황: 사용자 제작 Aircraft 1객체(속성 4종 string). **v0.1 대비 갭** = operator_ref·type 누락, is_military가 string, create-aircraft에 icao24 파라미터 없음(UUID 자동 = 엔티티 해소 깨짐 — 실 인제스트 최우선 블로커), Observation·링크·CreateAnomaly 미구축. 선행 UI 작업 4건 = P0B-foundry.md §8-5
- [architecture] **P7 하이브리드 이관 구현(부분)**: `FoundryOntologyStore`(write=액션/read=저수준 SDK) + `HybridStore`(Aircraft·Observation·observed_as→Foundry, 나머지→로컬) + `make_store()` 팩토리, `SKAI_STORE=foundry` 활성화(기본 local 불변). 단위 13 + 라이브 skip 2 테스트(총 119+2). read는 실 Foundry 검증 완료 (DR-0009, `P7-foundry-migration.md`)
- [ontology] Foundry 실측 갱신: Aircraft 속성 **v0.1 정합 완료**(사용자 보강 — isMilitary boolean 포함), Observation 신설(provenance 3필드 포함), observed_as는 FK(`aircraftIcao24`) 기반 링크쌍으로 구현됨. **단 create 액션 2종이 실행 실패 상태**(고아 required 파라미터 newParameter·newParameter1 의심) + PK/FK 파라미터 부재 → Foundry write 전면 블록. UI 수정 순서 6단계 = P7 §2
- [ontology] **Tier1 재검증 — 진단 반전**: `newParameter`는 고아가 아니라 **PK 바인딩 파라미터**(UI 오명명). 실 PK를 주면 create 4종 전부 동작, ObjectAlreadyExists로 PK dedup 강제 확인. 갭 0·1·2·4 해소. Foundry 스키마 = 객체 4종·링크 4쌍(observed_as만 FK, Tier1 3종 MANY-MANY)·액션 12종, OSDK 0.3.0(Object+Action 포함 발행). **잔존 최우선 갭 = evidence 강제 부재**(create-anomaly가 evidence를 안 받고 무근거 생성 성공 + 링크 채울 액션 부재 + confirm/dismiss 미구현) → 사용자 UI 목록 6건 = P7 §7-6 (`P7-foundry-migration.md` §7)
- [pipeline] **실데이터 이관 INGEST-OK**: store_foundry 파라미터 매핑 수정(실 PK·FK, placeholder 제거, ObjectAlreadyExists skip) 후 SKAI_STORE=foundry 1사이클 — Foundry Aircraft 3→6·Observation 0→3(실 icao24 hex PK), observed_as FK traverse 확인, dedup 재실행 카운트 불변. Anomaly·Region write는 **의도적 보류**(evidence·confidence 파라미터 부재로 provenance 손실 write가 되므로 — 원칙 위반). 테스트 126+2 (P7 §8)
- [ontology] **D1~D6 재검증(0.5.0)**: D-2 set-region-alert-level 해소(Modify 전이·팬텀 0) · D-3 신규 7타입 PK 해소(dedup 확인) · D-5 composed_of 해소(edit-observation.trackId) · D-6 self-link 해소. D-4 부분(of·Track→AC·Weather/Assessment→Region traverse OK / **over 미형성·within 채움 파라미터 부재**). **★D-1 잔존 = 유일 하드블로커**: `newParameter1`이 제거되지 않고 required objectSet\<Observation\>으로 재타입돼 Add-link 규칙이 ApplyActionFailed 유발(half-Anomaly 반복). 거부 검증 자체는 진전(무근거 INVALID). OSDK 0.5.0에 set-region-alert-level 누락(저수준 폴백 필요). 상세 P7 §10
- [architecture] 코드측 배선 가능 확정(7종 write + FK/composed_of + set-alert 저수준) → store_foundry 확장 착수. write_anomaly만 D-1 해소 대기
- [ontology] **B2 Staged Human Review 완료(STAGED-OK) = 초기계획 백엔드 전항목 종결**: 사용자 규칙 수정(0.12.0) 후 플랫폼 레벨 E2E — ★pending 중 본 explanation 불변·proposedExplanation에 AIP 산출(라이브)·approve 시 속성→속성 복사·reject 시 보존, 전부 Foundry 직독 검증. 게이트 SKAI_REVIEW=staged(기본 off). "AI가 제안하고 사람이 승인해야 온톨로지가 바뀐다"가 플랫폼에서 성립 — aip-integration §0 staged review 사실화
- [ontology] **B3 Automation 완료(정정)**: Automate emergency_squawk 감시 — Live Monitoring 발화·**알림 수신 확인**(Last run 2026-07-05 01:24, 13s). 초기 미발화 원인=조건 편집 중 알림 효과 탈락 → 재부착으로 해소. 함정 기록: linked object set은 live 불가(base set+속성 필터 필수). **pull(우리→Foundry)+push(Foundry→우리) 양방향 루프 실증**
- [pipeline] **A6 DB 레짐 3분리 확립**: skai.db=라이브 실데이터 전용(합성 21건 오염 백업 후 리셋 — surgical 기각 사유 기록) / demo/skai_demo.db=replay 합성 / demo/skai_foundry_local.db=Foundry 데모 로컬 미러. inject 가드(--allow-live-db 없이 라이브 db 주입 거부). **disjoint 해소**: 원인=assess가 물릴 로컬 db 불일치(dual-write 부재 아님) → 미러 자립화(region 추가)로 자연 흐름 E2E 성공(produced_by=aip_logic·Foundry 항적+로컬 anomaly 자연 합류·provenance 보존). 테스트 280 (db-regime.md)
- [architecture] **★AIP Logic 2종 완성 = 초기계획 aip-integration §2(4) 그대로**: 사용자 발행 `region-situation-summary`(0.10.0) 배선 — situation_summary 헤드라인 서술을 AIP가 생성(Anomaly Object set을 Foundry 참조로 전달 = 온톨로지 위 추론, 라이브 E2E 검증). **cites·kind 불변**(서술만 교체, provenance 유지), overallAssessment 메타 노출. 게이트 SKAI_COPILOT_LLM=aip + foundry 모드 한정, 실패·로컬·0건 전부 template 폴백. 실측 함정 기록: 입력명 anomalies2·optional 파라미터도 "" 필수(ReferenceHasNoValue). 테스트 280(+12). 주의: 자연 흐름은 이상징후가 dual-write로 양쪽 존재해야 함(데모 자산 disjoint 상태 문서화)
- [architecture] **★AIP Logic 실배선 = EVALUATION OVERSOLD#1 종결**: 사용자가 Foundry AIP Logic 함수 `explain-anomaly` 발행(0.9.0 OSDK 타입드 쿼리 포함) → `AipLogicExplainer` 스텁 제거·실구현. 근거 Observation을 **온톨로지 객체 참조**로 넘겨 AIP가 실제 속성(squawk·위치·고도·ts)을 읽고 설명·신뢰도 생성 — 라이브 실호출 검증(7700 비상 스쿽 설명 생성 확인). "설명은 AIP Logic이 생성"이 이제 사실. 어떤 실패든 template 폴백(DR-0004), 기본 template 불변(LLM 비결정성 실측 → replay 결정성 보존). **이 백엔드 한정 confidence=AIP 산출**(DR-0004 "confidence=룰" 대비 의도적 편차 — 정황·설명 백엔드로서 수용, 발표에선 "AIP가 설명·신뢰도 생성"으로 정확히). 테스트 247(+6)
- [ontology] **0.8.0 최종 재검증 = ★Foundry 완성도 라운드 종결**: UI 배치 ①~⑤ 전부 반영(correlatedWith Optional·속성 채움 파라미터 4종·trackId 바인딩·evidence 잔재 없음·region PK 소문자) + isMilitary boolean 정정. 코드 배선: correlatedWith placeholder 제거, **sentencesJson(문장별 cites Foundry 보존 = dual의 Foundry측 완성)**·station·groundTrackJson·attrsJson + read는 실속성 우선·구객체 폴백(무회귀). live=OSDK=36액션 완전 정합. 라이브 왕복 14검증 OK(over·within traverse 포함), anomaly 클린 생성(흡수 미발동), 순증 0. 테스트 184. 잔여 = assessment required object placeholder(스키마 미변경분)뿐 (P7 §17)
- [ontology] **0.7.0 재검증 = 완성도 라운드 수렴**: ★create-anomaly **클린 실행 해소**(사용자 규칙 수정 — 링크를 신규 객체로 재배선, ApplyActionFailed 국면 종료·흡수 코드는 방어용 유지·미발동 확인). **E-4 전면 리네임 완료**(create 11종 PK 실명화, edit-observation만 잔존) + store 16곳 동기. E-2: over·within 링크 형성, correlatedWith 엣지(단 required — placeholder 운용), mentions Optional화. E-3: 속성 6종 존재, createdAt·explainerBackend 배선(나머지 4종은 채움 파라미터 부재). **set-region-alert-level 0.7.0 포함**(3연속 누락 해소). 테스트 179. 잔여는 전부 선택(P7 §15-6)
- [pipeline] 데모 리허설발 크로스런 dedup 픽스 기록(§14): 실 Foundry ConflictError/errorName 연결형 매칭 — SKAI_STORE=foundry 반복 인제스트 전 경로에 유효한 코어 수정. 합성 데모 자산은 실행별 유니크 PK + 자동 정리(순증 0), 실 hex 인제스트분은 보존(Object Explorer 시연에 유리)
- [pipeline] **write_anomaly 배선 완료 = Foundry provenance 백본 코드 완성**: create-anomaly의 무해 ApplyActionFailed를 catch+read-back(객체+evidenced_by 엣지)으로 흡수. Anomaly dual-write(Foundry 스칼라+첫 근거 evidenced_by·involves 엣지 / 로컬=전체 링크 권위본), confirm/dismiss 상태 전이 dual 배선. 라이브 검증: 에러 흡수·양방향 traverse·confirm 전이 OK, Foundry 순증 0. 테스트 178. 잔여(전부 선택): correlated_with·다중 근거는 로컬 권위본(액션 파라미터 단수 한계), over/within 링크, 액션 후처리 에러 정리 (P7 §13)
- [ontology] **D-1 최종 재검증(0.6.0) = 실질 해소**: newParameter1·evidence(string) 제거 확인, 근거 경로가 observations(단일·required) 하나로 정리. **evidence 없으면 INVALID 거부 + evidenced_by 엣지 6/6 안정 양방향 형성 + involves 형성 = half-Anomaly 해소.** 잔존: create-anomaly EXECUTE가 매번 **무해한 ApplyActionFailed**(모든 edit 커밋 후 후처리에서 발생, 데이터 무손상 — 범인은 newParameter1이 아니었음 판명) → 코드에서 catch+read-back 흡수로 데모 백본 완성 가능. **데모 provenance 백본 Foundry 재현 = 조건부 가능으로 전환**(§10 "불가"에서). OSDK 0.6.0에 set-region-alert-level 연속 누락(발행 갭 — 저수준 폴백 유지). 상세 P7 §12
- [ontology] **전량 스키마 재검증(0.4.0)**: 사용자 전량 구축 후 실측 — **Object 11/11 속성 정합 완성**(전 객체 PK·속성·타입 스펙 일치). **단 Action층 결함**: ⓐ 신규 7타입 create에 PK 파라미터 전무(UUID 자동 = 엔티티 해소 불가), ⓑ create-anomaly가 Observation-evidence 파라미터 부재(핵심 근거 못 붙임) + aircraft/news/orbitpass 오강제 + valid refs로도 ApplyActionFailed(비원자적·half-생성), ⓒ set-region-alert-level이 Modify 아닌 Create로 오작동(팬텀 Region 양산), ⓓ of/over/within/composed_of 링크 미형성(속성만 존재), ⓔ self-link required junk. **정상 작동 확인**: confirm-anomaly(status 전이), 11객체 read/write, FK observed_as/operated_by. 데모 provenance 백본은 Foundry에서 아직 미완 — 남은 갭 A(UI 8건)/B(코드 5건) = P7 §9

## 2026-07-05

- [pipeline] **신호·화면 정직화 Phase A**(커밋 491568a, DR-0013): ① 위성 근접 승격·OrbitPass 상관을 **ISR-관련 위성 허용목록 48기**(Celestrak resource GP 실추출)로 게이트(기본 on·합성 우회·SKAI_SAT_ALLOWLIST=off 탈출구) — ISS 등 정상 통과의 경고 스팸 구조 차단, 기본 GROUP stations,visual→resource. ② correlated_with 링크에 **사유(시간차·거리·공유지역) 영속**(attrs_json ALTER·하위호환) + API 노출 — "🔗73이 뭔지 모르겠다" 해소 기반. ③ 코파일럿 시간 정직화: 뉴스 48h 상한+기사 나이 문장 표기, 위성 서술을 질의창 내로 제한(상관 계산은 ±1h 유지). ④ 뉴스: /api/news 48h 상한(replay 앵커 기준), "Korean Air"→"Korean Air Force" 오링크 정규식 수정, 뉴스 시각 날짜 표기, 미링크 기사 배지.
- [architecture] **신호·화면 정직화 Phase B**(DR-0013): ① 라이브 군용 식별 보강 — adsb.fi `/v2/mil` 스냅샷(60s TTL, 호출 1req/60s)의 dbFlags&1(readsb 문서 확정)로 군용 hex 집합 캐시, 우선순위 DB플래그 0.65 > 콜사인 휴리스틱 0.55 > 합성 명시, mil_reason이 설명문까지 전파. 게이트 SKAI_MIL_ENRICH=live(코드 기본 off, demo.sh live 기본 on). 오탐 실증(민간기 DB 오등록) 기록 — 저신뢰 원칙 유지. ② 합성 라벨 전면 전파: 지도 마커·cite 배지·서브그래프 노드·요약 헤드라인 "(합성 N건 포함)". ③ 설명 레이어: 서브그래프 모달 안내줄+엣지 한국어 병기, Anomaly 노드 원소스 링크(첫 evidence), Aircraft 노드 좌표 보강(기존 '지도에서 보기' 무동작 버그 수정), 소스 패널 소스별 설명·신선도 배지, 상관 리스트를 reason 기반 사유 문장으로(상위 3+접기). ④ demo.sh live에 SKAI_CROSSCHECK=live 기본화(dropout 교차확인 이중경로 실동작) + adsb.fi 크레딧 UI 상시 표기(ToS).
## 2026-07-08

- [docs] **README 전면 재작성** — 대회 정보·문제의식·접근(가설 5종과 검증 결과)·산출물 버전별·최종 결론(방어되는 것/한계)·방향을 객관·정직 기준으로. 발표·제출 자료는 `docs/hackathon/`으로 아카이브(demo.md·submission/). DR-0014(라이브 운영 라운드) 사후 기록 + 심각한 오류 3건(dropout 폭주·:8000 고아 유령응답·이상징후 무한누적) 교훈 명문화
- [docs] 발표·제출 자료(docs/hackathon/)와 배포용 도커 세트(Dockerfile.demo·requirements-demo.txt·entrypoint·.dockerignore) **삭제** — 터널 채택으로 도커 경로 실사용 0, 대회 종료로 발표 자산 불필요(사용자 지시). git 이력(4dab580 이전)에 보존
- [pipeline] `demo.sh stop` 고아 스윕 보강 — pid 파일 한계로 크래시 경유 고아 프로세스가 :8000을 점유하던 버그(07-06 실측) 수정

## 2026-07-06

- [pipeline] **adsb.fi 항적 대체 소스**(DR-0014 #1): OpenSky 익명 크레딧 소진(429, UTC 자정 리셋) 긴급 대응 — `/v2/lat/lon/dist` 반경 2점(각 250nm)로 KADIZ bbox 커버, 응답 키·단위 실측(aircraft/resultCount, ft→m·kt→m/s·ft/min→m/s 정규화 — 급기동 룰 임계 정합), SKAI_POLL_SOURCES=adsbfi로 opt-in(기본 불변·opensky 공존 가능). 교차확인은 동일 소스 동어반복 방지로 off 원칙(DR-0014 #2)
- [pipeline] **군용기 지도 가시화**(DR-0014 #3): 판정을 Aircraft.is_military로 영속(단조: 기존 True 불변 > adsb.fi /v2/mil dbFlags&1 60s 스냅샷 > 콜사인·대역 휴리스틱) → /api/observations·tracks 노출 → 지도 올리브 마커+'군용 추정(저신뢰)' 툴팁. 실군용기 라이브 포착 검증
- [docs] 팀원 온보딩 실검증: requirements.txt 신설(실측 8패키지), 격리 클론→venv→replay·live·테스트 전과정 통과, test_stealthmole의 .env 실키 암묵의존 버그 수정, TEAM.md에 Foundry 실사용 온보딩(접근 경로·본인 토큰 원칙·실사용 메뉴)
- [pipeline] 운영 구성: METAR 5곳(METAR_ICAOS)·라이브 인스턴스 합성 제거(순수 실데이터, replay는 유지)·소스 패널 라벨 'ADS-B 항적' 중립화. 심각한 오류 2건 기록(DR-0014 §오류: dropout 폭주→폴러 마비, :8000 고아 프로세스 유령 응답)
- [ontology] **이상징후 라이프사이클 — status에 `resolved` 추가**: dropout 후보의 주장("신호 끊김")이 반증되면(해당 기체 복귀 관측) 시스템이 자동 해소 — 복귀 관측을 evidenced_by로 연결 + attrs.resolution 계약(kind/obs_id/resolved_at). 사람 결정(confirm/dismiss)은 불변·불가침, replay 자산은 폴러 미경유라 candidate 유지(테스트 증명). 배경: 룰 수정 후에도 시간당 ~78건 순증(전부 candidate, 닫는 경로가 사람 클릭뿐). + **경계이탈 억제**: 마지막 관측이 민감구역 union bbox 가장자리(마진 0.5°)+기수 바깥 방향이면 커버리지 이탈로 보고 후보 미생성. Foundry는 resolve 액션 부재로 미러 안 함(로컬 권위, 갭 기록). 테스트 422. UI: resolved 배지·접힌 그룹·6h 시간창 + **트랙 경로 기본 숨김**(선택 기체만 표시·전체 토글, gap 트랙은 서사용 상시 표시)
- [ontology] **dropout 의미 재정의 = 라이브 오탐 폭주(408건 중 400) 수정**: 원인 3중 — gap 임계 90s 고정(60s 폴에서 전 트랙 gap화) + has_gap 이력 고착(sticky) + dedup 10분 버킷 재발화(송신 중 기체가 10분마다 새 Anomaly). 수정: ① `gap_threshold_seconds()`=max(90, 3×SKAI_POLL_INTERVAL)(model.py SSOT) ② 발화 게이트를 "**지금** 침묵"(now−마지막 관측>임계, 활성 창 30분)으로 — 신선한 관측이면 과거 gap 무관 비발화 ③ dedup을 침묵 시작에 정확 앵커(침묵 이벤트당 1건, 복귀 후 재침묵은 새 이벤트) ④ 착륙 억제(on_ground 억제·저고도 0.3 하향). has_gap은 경로 재구성 표식으로 유지. replay 합성 3/3 발화 보존, 테스트 409. architecture.md §3·ontology.md §4 dropout 행 동기화. 스키마 변경 없음(룰·파생속성 계산만)
- [docs] demo.md 실측 갱신(2026-07-05 재검증): replay 이상징후 **13건·6유형**(급기동 포함)·항적 16대·문장 54개 전부 cites·재기동 후 바이트 동일 재현 확인. 군용기 이중경로 프레임(켠 군용기=저신뢰 식별/끈 군용기=dropout 부재 탐지)·위성 허용목록 정직성 블록 추가. 스텝① 발화에서 고정 숫자 낭독 제거(화면이 증거).
