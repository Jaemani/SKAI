# replay 데모(scripts/demo.sh replay) 실행에 필요한 최소 의존성.
# import 추적(server.app · scripts.inject_synthetic · scripts.scenarios · ontology/* ·
# copilot/* · anomaly/* 의 실 import 그래프)으로 확인 — 전체 .venv freeze가 아니라
# replay 경로가 실제로 쓰는 패키지만. Foundry OSDK(foundry_sdk·skai_osdk_sdk·
# foundry_sdk_runtime)는 ontology/store_foundry.py·anomaly/explainer.py 안에서
# SKAI_STORE=foundry일 때만 함수 내부 lazy import되므로 제외(메인 .venv에도 미설치).
# sgp4·httpx·PyYAML·PyJWT·python-dotenv 등은 connectors/*(live 폴러) · scripts/p7_*
# (Foundry 검증 스크립트) 전용이라 replay에는 불필요.
#
# 버전은 .venv(python 3.14) 실측 freeze와 동일 핀(2026-07-05 기준).
fastapi==0.139.0
uvicorn==0.49.0
pydantic==2.13.4
